<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- API 33+: required to show the model-download progress notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Floating dictation bubble drawn on top of other apps. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Speech">

        <activity
            android:name=".LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- configChanges: handle rotation/UI changes in-place instead of
             recreating the Activity, which would build a second SpeechPipeline
             (heavy native models) and leak the old one. -->
        <activity android:name=".MainActivity" android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode" />
        <activity android:name=".DictationActivity" android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode" />
        <activity android:name=".SpeechRecognizerTestActivity" android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode" />
        <activity android:name=".overlay.VoiceOverlayActivity" android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode" />

        <!-- Holds the floating bubble and the mic while the user dictates in
             another app, so it must be a microphone-type foreground service. -->
        <service
            android:name=".overlay.OverlayBubbleService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Types the transcript into the focused field of whichever app is
             in the foreground. -->
        <service
            android:name=".overlay.DictationAccessibilityService"
            android:exported="true"
            android:label="Speech dictation"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/dictation_accessibility_service" />
        </service>

        <!-- Settings entry the system Voice-input picker opens via the gear -->
        <activity
            android:name=".SpeechRecognitionSettingsActivity"
            android:exported="true"
            android:label="Speech recognition settings">
            <intent-filter>
                <action android:name="android.speech.action.RECOGNIZER_INTENT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name="audio.soniqo.speech.service.SpeechRecognitionService"
            android:exported="true"
            android:permission="android.permission.RECORD_AUDIO">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
            <meta-data
                android:name="android.speech"
                android:resource="@xml/recognition_service" />
        </service>

        <service
            android:name="audio.soniqo.speech.service.SpeechTextToSpeechService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TTS_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.speech.tts"
                android:resource="@xml/tts_engine" />
        </service>
    </application>
</manifest>
